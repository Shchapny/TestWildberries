<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TestWildberries" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/wildberries</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="LikeButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:checkable">true</item>
        <item name="iconTint">@color/like_color_selector</item>
        <item name="icon">@drawable/like_selector</item>
        <item name="android:layout_height">@dimen/size_like</item>
        <item name="android:layout_width">@dimen/size_like</item>
        <item name="android:background">?android:attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="TextPrice" parent="TextAppearance.Material3.ActionBar.Subtitle">
        <item name="android:textColor">@color/color_price</item>
        <item name="android:textSize">@dimen/text_size_price</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextLocation" parent="TextAppearance.Material3.ActionBar.Subtitle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/text_size_location</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/wildberries</item>
    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>